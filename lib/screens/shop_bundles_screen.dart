import 'package:flutter/material.dart';

class ShopBundles extends StatelessWidget {
  const ShopBundles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
